steps:
  - label: "Build NuGet Package"
    key: "build-nuget-package"
    artifact_paths: "Hello.Buildkite.*.nupkg" # upload to build artifact storage
    plugins:
      docker-compose#v3.8.0:
        run: dotnet
        config: .buildkite/docker-compose.yml
        command: "true"
